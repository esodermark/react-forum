import styled from 'styled-components'

export const Button = styled.button`
  width: 10rem;
  padding: .7rem;
  background: var(--primary-button);
  border: var(--border);
  font-size: 1.6rem;
  cursor: pointer;
`