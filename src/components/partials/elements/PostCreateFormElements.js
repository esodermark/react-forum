import styled from 'styled-components'
import { BaseTextArea } from '../../baseElements/Input'

export const PostCreateContainer = styled.div`
    
`

export const PostCreateWrapper = styled.div`
    width: 40%;
    margin: 8rem auto;
    background: var(--light-gray);
    padding: 5rem;
    border: var(--border);
`

export const PostCreateFormForm = styled.form``

export const TextArea = styled(BaseTextArea)`
    margin-top: .5rem;
    margin-bottom: 2rem;
`